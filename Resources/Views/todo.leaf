#extend("base"):    #export("body"):    <div class="container">        <h1 class="text-white text-center p-3">#(todo.title)</h1>        <form method="POST" action="edit">          <div class="input-group mb-3">              <input type="text" name="title" class="form-control" value="#(todo.title)">              <div class="input-group-append">                  <input type="submit" value="Save" class="btn btn-success">              </div>          </div>        </form>                <table class="table table-bordered bg-white">          <tbody>          <tr>            <th>                <div class="container">            <div class="row">            <form id="form_tags">                    <label>Tags:</label>                    <select name="selected">                    #for(tag in tags):                        <option value="#(tag.id)">#(tag.name)</option>                    #endfor                    </select>                    <button type="submit" form="form_tags" formaction="./todotags" formmethod="POST" class="btn btn-sm btn-primary">Add tag for todo</button>                    <button type="submit" form="form_tags" formaction="/tags" formmethod="GET" class="btn btn-sm btn-info">Edit</button>            </form>                                                </div>                </div>            </th>          </tr>            #for(todotag in todoTags):              <tr>                  <td>                    #(todotag.name)                  </td>                  <td width="50">                    <form action="./todotags/#(todotag.id)/delete" method="POST">                        <button class="btn btn-sm btn-danger">Remove</button>                    </form>                  </td>              </tr>              #endfor          </tbody>      </table>      <table class="table table-bordered bg-white">          <tbody>          <tr>            <th>Details</th>          </tr>            #for(detail in details):            #for(description in detail.descriptions):              <tr>                  <td>                    #(description)                  </td>                  <td width="50">                    <form action="./todos/#(todo.id)/delete" method="POST">                        <button class="btn btn-sm btn-danger">Remove</button>                    </form>                  </td>              </tr>              #endfor              #endfor          </tbody>      </table>                  #endexport#endextend